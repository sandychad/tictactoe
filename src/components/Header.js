import React from "react";

const style = {
  textAlign: "center",
};

const Header = () => {
  return (
    <header style={style}>
      <h1>Tic-Tac-Toe</h1>
    </header>
  );
};

export default Header;
